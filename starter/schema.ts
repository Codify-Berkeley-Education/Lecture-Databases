import {
  int,
  sqliteTable,
  text,
  uniqueIndex,
  index,
  primaryKey,
  real,
} from "drizzle-orm/sqlite-core";
import { sql, relations, type SQL } from "drizzle-orm";
import { ulid } from "ulid";

// Users

// Posts

// Comments
