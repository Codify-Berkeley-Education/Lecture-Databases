import {
  int,
  sqliteTable,
  text,
  index,
  real,
  uniqueIndex,
  primaryKey,
} from "drizzle-orm/sqlite-core";
import { sql, relations, type SQL } from "drizzle-orm";
import { ulid } from "ulid";

// Users

// Posts

// Comments
